# Backend du réseau social Groupomania

Pour installer les dépendances utiliser la commande `pnpm install`

Pour lancer le serveur utiliser la commande `pnpm start`

### A inclure dans un fichier .env :
- DB_HOST
- DB_USER
- DB_PASSWORD
- DB_NAME
- TOKEN_ENCRYPT